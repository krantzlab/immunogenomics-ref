# R artifacts
.Rhistory
.Rdata
.RData
.Rproj.user/
*.Rproj

# OS files
.DS_Store
Thumbs.db

# Editor artifacts
*.swp
*.swo
*~
.vscode/
.idea/

# R package build artifacts (if ever packaged)
*.tar.gz
*.Rcheck/

# pixi
# pixi.lock is committed deliberately -- it is what pins the interpreter and
# package versions, so `pixi run validate` means the same thing everywhere.
.pixi/

# Project-local R library for CRAN packages with no conda-forge build
# (HLAtools). Rebuilt by `pixi run -e managed deps`.
.Rlib/

# Claude Code
.claude/

# Temporary files
tmp/
temp/